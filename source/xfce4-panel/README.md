# HUD-04 — panel / notify / launcher

Source of truth. Live copies:

- `~/.config/xfce4/xfconf/xfce-perchannel-xml/xfce4-panel.xml`
- `~/.config/xfce4/xfconf/xfce-perchannel-xml/xfce4-notifyd.xml`
- `~/.config/gtk-3.0/gtk.css`
- `~/.themes/Vault.OS/gtk-3.0/hud.css` (imported by GTK-02 gtk.css)
- `~/.themes/Vault.OS/xfce-notify-4.0/gtk.css`

Stock XFCE only. Launcher is `applicationsmenu` titled VAULT.
`whiskermenu-1.rc` is staged; do not enable without `xfce4-whiskermenu-plugin` and human OK.
