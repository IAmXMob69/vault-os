# Icons

Live theme: `~/.icons/Vault.OS` (source under `source/icons/`, packaged copy under `icons/Vault.OS/`).

Plates use inset fill, a 1px bevel, and phosphor ink (`#1AFF6B`). Rad and amber always ride with shape. The theme inherits `hicolor` only — session launchers must not pin FalloutMojave or PipBoy-NV; keep launchers on Vault.OS, not FalloutMojave/PipBoy-NV.

Cursor theme ships beside the icons (`cursors/`, 24px). Wallpaper: `source/wallpapers/vault-111.png`.
