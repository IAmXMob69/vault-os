# Icons

Theme directory: `icons/Vault.OS` (install into `~/.icons/Vault.OS`).

Phosphor green on dark plates, 1px bevel, cursors included. Point XFCE at Vault.OS for both icons and cursors after you copy the tree — or run `./bin/vault-os install`.
